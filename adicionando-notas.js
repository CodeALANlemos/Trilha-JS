const notas = [10, 6, 8];
notas.push(7); // o nome da variável.push serve para adicionar mais um elemento na array, adionando o elemento na ultima posição. E tem que ser colocado 
//algum parâmetro dentro da variável.

console.log(notas);

let media = (notas[0] + notas[1] + notas[2] + notas[3]) / notas.length

console.log(media);
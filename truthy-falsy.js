//boolean
const usuarioLogado = true;
const contaPaga = false;

//truthy ou falsy : não são exatamente booleanos, mas podem se comportar como se fossem.

// 0 e 1 são valores que o JavaScript e em algumas linguagens, fazem o papél de verdadeiro e falso.

// 0 => false;
// 1 => true;


// null ==> vazio ou nada;
// undefined;

let minhaVar;
let varNull = null;

let numero = 3;
let texto = "Alura";

console.log(typeof numero); // o typeof serve para identificar que tipo é aquela variável.
console.log(typeof texto);
console.log(typeof varNull); // var null vai retornar um objeto, sendo um bug do próprio JS. 

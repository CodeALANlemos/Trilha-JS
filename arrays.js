/*let nota1 = 10;
let nota2 = 6.5;
let nota3 = 8;
let nota4 = 7.5;

let soma = nota1 + nota2 + nota3 + nota4;
let media = soma / 4;

console.log(media); */

// com arrays

const notas = [10, 6.5, 8, 7.5];

let media = (notas[0] + notas[1] +  notas[2] + notas[3]) / notas.length;

console.log(media);

// tipos de arrays: 
/* const arrayDeNumeros = [50, 43, 12];
   const arrayDeStrings = ["banana", "alura", "Juliana"];
   Lembrando que cada elemento corresponde a um dado, separados por vírgula. O espaço é opcional, mas facilita a leitura.
   Cada elemento está localizado em uma posição na lista, chamada de índice:Em programação, chamamos de índice (em inglês, index) o número que identifica a
   posição de um valor em um array.
*/
